package com.rc.raceproject.repository;

import com.rc.raceproject.entity.Parenthorses;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParentHorseRepository extends JpaRepository<Parenthorses, String> {
}
